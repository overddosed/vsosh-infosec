```
## СЗИ-3 + Форензика (9 кл)

СЗИ 3 - Сетевые движения

Товарищ! На '_Предприятии 3826_' зафиксирована подозрительная активность в сетевой инфраструктуре. Робот отправляет опасные пакеты, вызывая сбои в системе управления. Наши детекторы уловили сетевые пакеты, но из-за остановки нейроИИ мы не можем их проанализировать. Твоя задача — разобрать сетевую активность в нашей сети, выявить все этапы возможной кибератаки и написать отчет “наверх” об атаке.

ВАЖНО: IP-адрес атакующего и флаг - индикатор решения задания, работы участников, некорректно \ не определивших его - не подлежат дальнейшей проверке! Решение внести по ссылке:(https://forms.yandex.ru/u/67d26ca602848fb03dfa940a/)
Рекомендуемые утилиты: Wireshark, hashcat
Цель работы: исследование вредоносной активности в записи трафика
Итог работы:
1. Сданный в тестовую систему IP-адрес атакующего и Флаг в формате **IP:flag**, например если IP атакующего: 182.60.24.12, а флаг VSOSH{test_flag} то в систему внести следует 182.60.24.12:VSOSH{test_flag}
2. Заполненный шаблон (по ссылке выше) с анализом хода атаки и заполненными полями.
**Критерии оценки:** • Корректно определен IP-адрес атакующего - 1 балл • Корректно определен флаг - 1 балла • Корректно описана цепочка проведения атаки - 4 балла • Корректно определен пароль пользователя mpalledorous - 1 балл
```

Решение:

Открываем предоставленный `pcap` файл в приложении для анализа сетевого траффика - `Wireshark`.

Путём не сложных вычислений, видим через `Conversations` несколько айпишников, первые 3 у нас ничего из себя особо не представляют, просто имеют какое то отношение к серверу, может на них расположены какие то сервисы или это банально бекэнд.

Замечаем, что среди двух айпишников наибольшее количество "разговоров" и понимаем, что они и являются атакующим и сервером. 

<img width="1365" height="677" alt="image" src="https://github.com/user-attachments/assets/3c3ae16c-4d95-48a4-aa8c-01bb0e995d47" />


На самом деле, если присмотреться поближе, то можно заметить, что айпи `172.16.8.120` лишь "притворяется сервером", видимо, чтобы заставить настоящий сервер подключиться к нему и с помощью этого злоумышленник проэксплуатирует какую либо уязвимость и получит условный `Reverse Shell` или `Reverse Connection`.

**Укажите IP-адрес атакующего:**
*`172.16.8.120`*

**Опишите, как вы определили IP-адрес:**
*Открыв вкладку `Conversations` в `Wireshark`, во время анализа сетевого траффика, я обнаружил, что `IP-адрес` "`172.16.8.120`" "притворяется сервером", то есть слушает и можно заметить как меняется его порты на "стандартные порты для сервисов" - например `8888` или `4443`, а после этого большинство пакетов исходит именно от этого айпишника к серверу - `172.16.8.20`. Также именно айпишник `172.16.8.120`, первым отправил `SYN` запрос серверу. Исходя из этого наблюдения я сделал вывод, что `IP-адрес` атакующего это `172.16.8.120`*

Идём дальше. Нужно найти флаг.

**Укажите флаг в формате VSOSH{...}:**
`*VSOSH{S4m_!}*`

**Опишите, где был обнаружен флаг:**
*Флаг был обнаружен во время анализа `tcp` трафика,  а конкретно можно отфильтровать с помощью поиска: "`tcp.stream eq 110`" и мы получим одни из самых интересных пакетов в этом таске, злоумышленник использовал эксплойт `mimikatz` и будучи в системе windows записал флаг, который можно увидеть в формате `base64`, а далее декодировать и получить флаг.*

<img width="1120" height="791" alt="image" src="https://github.com/user-attachments/assets/7e622142-5a74-4bf1-8797-73001e06b0a9" />

Далее нужно описать весь ход проводения атаки в 5 этапов и приложить скриншоты с подробными объяснениями, а после этого указать пароль для пользователя *`mpalledorous`*,
нужно будет взломать хеш этого пользователя, который можно найти проанализировав всё тот же `tcp stream`:
```
User : mpalledorous
===========================================
Hash NTLM: 3bb874a52ce7b0d64ee2a82bbf3fe1cc
```

Далее нужно будет описать, как был найден пароль. То есть полностью описать, например *анализ `NTLM-хешей` в дампе `Mimikatz`, использование `Hashcat` с `wordlist`*. И после этого приложить скриншоты процесса взлома.

А потом нужно придумать какое то правило `iptables` для блокировки *аналогичных обращений*, я так думаю, что нужно написать правило, которое будет неким фаерволом, чтобы злоумышленник не смог провести такую атаку и притворится сервером, заставив сервис подключаться к нему и благодаря этому получить реверс коннекшн.

В данный момент - 07:33 у меня не работает виртуал бокс, поэтому я пойду решать другой таск на анализ трафика, а потом вернусь сюда и нужно будет, что-то думать по поводу проведения атаки злоумышленником и как вообще это всё расписать.

Итак, продолжаем теперь глубокий анализ.

Задание:
```
### 

Опишите этапы атаки в хронологическом порядке.

Обязательное поле

Примерная структура:  
Этап 1  
Примеры пакетов: [номера].  
Доказательства: [скриншоты/описание].

Этап 2  
Примеры пакетов: [номера].  
Доказательства: [скриншоты/описание].

Этап 3  
Примеры пакетов: [номера].  
Доказательства: [скриншоты/описание].

Этап 4  
Примеры пакетов: [номера].  
Доказательства: [скриншоты/описание].  
Получение учетных данных

Этап 5  
Примеры пакетов: [номера].  
Доказательства: [скриншоты/описание].

Примечание: количество этапов может отличаться на 1-3
```

Этап 1 `Reconnaissance` 
Мы видим, что `IP-адрес 172.16.8.120` открывает множество сессий подряд, на это намекают разные номера портов к одному и тому же порту сервера, а также время подключения либо одинаковое, либо разница в секунды. Ниже приведён пример трёх таких запросов, но на самом деле их примерно `10`~ Тут злоумышленник просто сканирует сервер, мы видим либо перебор, либо автоматизированные запросы за короткий промежуток времени. Также можно обратить внимание, что атакующий отправляет запросы конкретно на страницу логина в попытках найти вектор для атаки.

<img width="1914" height="723" alt="image" src="https://github.com/user-attachments/assets/344f7d0d-1509-4c4d-ae27-cc41a686051f" />


Этап 2 `Exploitation`
Далее пролистав чуть ниже пакетов сканирования, можно заметить интересный теперь `POST` запрос на ту же страницу логина, но уже с ответом `302 Found`. Проанализировав данный пакет, можно заметить, что злоумышленник вошёл в систему используя данные -
`Administrator:P@ssw0rd123!` благодаря чему получил доступ к управлению над всем сайтом, что можно заметить, если обратить внимание чуть ниже на запрос под названием:
`GET /API/personaBar/localization/gettable?culture=en-US HTTP/1.1`, исходя из такого запроса делаем вывод, что атакующий получил доступ к `API` системы - `personaBar` (см. `AdminPanel.png`), что является панелью управления. Если проследовать этому пакету, то мы можем убедиться, что это панель управления, ведь мы видим `json` ответ, в котором если внимательно присмотреться, можно заметить настройки для просмотра логов `AdminLogs`, благодаря чему атакующий может стереть свои следы в логах. Далее злоумышленник отправляет запрос в панель, чтобы получить информацию о пользователях:

`GET /API/PersonaBar/Users/GetUsers?searchText=&filter=0&pageIndex=0&pageSize=10&sortColumn=&sortAscending=false HTTP/1.1`
Успешно получает информацию о некоторых пользователях и может им изменять права доступа. (скрин прикладывать не буду, информация имеет мало отношения к флагу, это лишь небольшая пост эксплуатация полученной админки) Ответ можно просмотреть, открыв пакет `227`

<img width="1906" height="724" alt="image" src="https://github.com/user-attachments/assets/119f51b4-5a4a-498d-9339-862cf83c0ea5" />

<img width="1919" height="900" alt="image" src="https://github.com/user-attachments/assets/a7c0d6ea-ee5b-4263-993a-aba59bec4cde" />

Этап 3 `Post-Exploitation`
Теперь хакер пытается получить доступ к `SqlConsole`, делая `POST` запрос `/API/personaBar/UserSettings/UpdateUserSettings` и видимо изменяя некоторые значения в панели, это можно заметить при анализе пакета `252`.
Далее злоумышленник получает доступ к `SqlConsole` и пытается получить `RCE` через `xp_cmdshell` - пакеты `293`, `295` и делает вот такой запрос `{"connection":"SiteSqlServer","query":"xp_cmdshell 'whoami'"}`, на что получает ответ о том, что `sql` server заблокировал доступ к `sys.xp_cmdshell`, потому что этот компонент отключен в конфигурации безопасности сервера. И злоумышленник благополучно идёт подкручивать себе права, изменять конфиг сервера. Далее хакер отправляет запрос к `GetSslSettings` и благодаря этому убеждается, что сервер на дефолтных настройках и видит параметр `"SSLEnabled": false`, что говорит о том, что трафик не шифруется, а просто идет по http и можно снять запрет на `xp_cmdshell` командами через `SQL`. Пакет `331`, `332`.
Далее хакер проверяет, какие выставлены настройки на сервере, чтобы понять есть ли еще какие либо запреты, чтобы успешно получить `RCE`. После этого злоумышленник подготавливается к загрузки вредоноса и добавляет значения `exe`, `asp` в `"AllowedExtensionWhitelist"` в настройках. 

<img width="1918" height="723" alt="image" src="https://github.com/user-attachments/assets/b041520b-f5cd-4cfe-91de-80ab88ce84a8" />


Далее можно заметить как злоумышленник переходит на страницу `/admin/file-management` - пакет `482`, просматривает различные модули на сайте и в конце мы видим `POST` запрос, в котором злоумышленник анализирует содержимое папок:
`POST /API/DigitalAssets/ContentService/GetFolderContent HTTP/1.1`
(см. скрин `Folders.png`)

<img width="1900" height="802" alt="image" src="https://github.com/user-attachments/assets/1d8ece4f-58b7-4ca4-8841-f7304d036bb9" />


И НАКОНЕЦ, видим как злоумышленник загружает свой `web-shell` (пакет `587`) под названием "`sgu3chen4a.asp`" и сервер отвечает `200` и выдаёт путь по которому был загружен файл (пакет `593`). И далее мы можем видеть, как злоумышленник отправляет команды в `cmd` через `get` запросы (пакет `616`), используя свой `веб-шелл`:
`GET /Portals/0/sgu3chen4a.asp?cmd=whoami HTTP/1.1`
и сервер всё обрабатывает и выдаёт `200` (пакет `619`)

4 этап `OS Post-Exploitation`
Далее мы можем отслеживать полное общение хакера с `cmd` по `tcp.stream eq 110` в `Wireshark` (пакеты `1558-...-1816`).
Хакер вводит `whoami` и получает в ответе `nt authority\system`, это означает, что у хакера полный контроль над системой. Далее хакер эксплуатирует программу `mimikatz.exe` и проверяя `privilege::debug` получает ответ "`OK`", это означает, что у него есть доступы к системным процессам; потом хакер выгружает базу данных `SAM` - с помощью `lsadump::sam`, где хранятся пароли пользователей. В итоге он получил `NTLM` хэши пользователей:
`Administrator` и `mpalledorous`.
`NTLM hash` пользователя `mpalledorous` - `3bb874a52ce7b0d64ee2a82bbf3fe1cc`
После этого он перемещается по системе и в директории `C:\Users\Administrator\Desktop`, записывает файл под названием `flag.txt`, значением которого является закодированная в `base64` строка: `VlNPU0h7UzRtXyF9` декодировав которую, мы получаем флаг.
`VSOSH{S4m_!}`
Потом хакер еще выводил команду `ipconfig`, где виден его `IPv4 172.16.8.20` и другая информация о сети

<img width="1784" height="776" alt="image" src="https://github.com/user-attachments/assets/de7c85bc-ab5c-43ed-99d8-3280068ddecd" />


Для того, чтобы взломать предоставленный `NTLM hash` используем рекомендуемую в таске утилиту `hashcat` с флагами `-a 0` для перебора и `-m 1000` так как это `NTLM hash`, создаем файл формата `.hash` и записываем туда наш хеш и добавляем в команду, а также используем словарь `rockyou.txt`, для вывода только нужной информации добавляем флаг `--show`
Получаем пароль пользователя `mpalledorous`

`3bb874a52ce7b0d64ee2a82bbf3fe1cc:1squints2`

<img width="774" height="69" alt="image" src="https://github.com/user-attachments/assets/635da9e4-2cb2-4779-a41b-73557966e3c6" />

**Создано правило iptables для блокировки аналогичных обращений**
**Приведите правило iptables**
`iptables -A INPUT -p tcp --dport 80 -m connlimit --connlimit-above 10 -j DROP`


