```
## Web-2 (9 класс)

Наша продуктовая компания хотела сделать космонавтов счастливее, написав удобный сервис для покупки котиков. Целью работы является исследование логики работы веб-приложения. При этом:

• Требуется определить недостаток в реализации веб-приложения;

• Необходимо прочитать флаг который является параметром конфигурации одного из модулей приложения. Это можно сделать разными путями в том числе и получив полный доступ к серверу.

Итог работы: Нахождение флага, доступ к которому является индикатором успешного решения задания.

Критерий оценки: Сдача корректного флага.

Рекомендуемые предустановленные утилиты: BurpSuite
```

Запускаем задание, переходим по ссылке и видим вот такую страницу:
![[Pasted image 20260510131407.png]]

Можно потыкать по кнопке `Buy` на каждого котика и в заголовке страницы видим текст:
`Now you are the owner of Lovely third cat!!! Thank you`
И кнопка вернуться на главную страницу.
![[Pasted image 20260510131626.png]]
Каждый котик по своему называется и при нажатии на других котов можем видеть точно такой же текст, но вместо `Lovely third cat` - отображается `Gorgeous first cat` или `Beautiful second cat`. На этом заканчивается весь функционал веб-сервиса.
Важно, что при нажатии на кнопку `Buy` мы отправляем `POST` запрос.
Чтобы гораздо отчетливее понимать какие запросы мы отправляем на сервер, воспользуемся утилитой `BurpSuite`:
Откроем `Proxy`, `Open Browser` и перейдём на веб-страницу, потыкаем на все кнопки.
![[Pasted image 20260510132142.png]]

Видим, что для каждого кота в теле `POST` запроса мы отправляем параметр `what+cat=` и с разными значениями -> `Gorgeous+first+cat`; `Beautiful+second+cat`; `Lovely+third+cat`
В переменную `what+cat=` мы можем записать всё что угодно, тем самым изменив значение, которое передаётся на сервер.
![[Pasted image 20260510133314.png]]

Поскольку то, что мы вбиваем в параметр `what+cat` отображается в `html` страничке, должно говорить нам о том, что скорее всего здесь есть уязвимость, которая называется `XSS`
`XSS - Cross-Site Scripting`, это `client side` уязвимость, которая позволяет вставлять `javascript` код и обрабатывать его. 
Для демонстрации введём `javascript` код, который выведет диалоговое окно с символом "`1`"
```xss
<script>alert(1)</script>
```
![[Pasted image 20260510134300.png]]
Действительно, здесь есть такая уязвимость, но дело в том, что `XSS`, это опять же `client side `уязвимость, то есть мы не можем на сервере исполнять команды (`ls`, `cat`, `id`), мы можем через `xss` например воровать `cookie` пользователей, авторизовываться на сайте, но тут не записываются `cookie` и нет окна авторизации. Поэтому нам нужен другой вектор атаки.
В описании к задаче напрямую говорят о том, что мы можем получить полный доступ к серверу, значит будем смотреть ещё.
Перейдём на главную страницу, отправим `GET` запрос через `burpsuite` и нажмём ещё по какой нибудь кнопке `Buy`. Отправим запрос с любым значением.
![[Pasted image 20260510135109.png|641]]
Видим, что веб-сервер расположен на питоновской библиотеке для веб-приложений `Werkzeug` и что сайт написан на питоне. `Werkzeug` написан на фреймворке `Flask`, он в свою очередь поддерживает шаблонизаторы.
Что такое шаблонизаторы? 
Условно есть у нас такая вот `html` страница:
![[Pasted image 20260510135647.png]]
Здесь мы вызываем шаблон, прописываем:
`{{ title }}` ; `{{ heading }}` ; `{{ content }}`

Перебираем элементы:
`{% for item in items %}`, отображаем их -> `{{ item }} {% endfor %}`

Есть строка "`Добро пожаловать, {{ user }}!`" вытаскиываем имя `user`
И "`Пожалуйста, href="/login"войдите в систему.`"

И вот если открыть условно мини-приложение на `Python`, на `Flask`
![[Pasted image 20260510140129.png]]
Мы заметим, что у нас есть словарь с ключами `'title'` ; `'heading'` ; `'content'` и т.д 
И благодаря этому можно понять, что происходит, то есть мы через шаблонизаторы (шаблоны), вот такие фигурные скобки `{{ }}` можем вытаскивать, например параметр `title` из нашего приложения на питоне и подставлять её в `html`.
То есть шаблонизаторы позволяют не прописывать `html` код непосредственно в питоне, а именно использовать `html` страницы, обращаться через `html` на сервер и вытаскивать параметры, переменные, функции вызывать как бы в коде питона.
То есть если мы всё это выполним, то получим вот такой результат:
![[Pasted image 20260510141515.png]]
То есть здесь всё подставится как в словаре с ключами. 
Также важно понимать, что шаблонизаторы на `Flask` используются через технологию `Jinja2`
Так называются шаблонизаторы на `Flask`.
Уязвимость связана с шаблонизаторами, попробуем проэксплуатировать.
Перейдём в `BurpSuite` и в значение переменной `what+cat` введём какой нибудь стандартный шаблон, например `{{ 5+5 }}` ( шаблон вызывается через фигурные скобки - `{{ }}}` ), если мы в ответе сервера получим `10`, значит уязвимость есть.
Получаем ошибку, но по крайней мере сервер попытался обработать этот запрос, что уже говорит о уязвимости:
![[Pasted image 20260510194231.png]]
Возможно нужно было немного другой шаблон прописать:
попробуем `{{5-5}}`
![[Pasted image 20260510194439.png]]
И да, в ответе получаем `0`, видимо на сервере есть какое то экранирование именно `+`, возможно потому что система принимает `+` за пробел в `Burpsuite`, потому что если ввести 
`{{5 5}}`, то получим такую же ошибку.
![[Pasted image 20260510194722.png]]
Да, и вправду `+` считается за пробел, наверное потому что отправляем через `BurpSuite`, ведь если сделать `URL encode` `{{5+5}}`, то всё будет нормально и получим `10`.

Вообщем тут есть уязвимость под названием `SSTI`
`SSTI - Server-Side Template Injection`, это инъекция шаблонов, причем на сервер (`Server-side`), тоесть мы можем отправлять команды на сервер.
Есть ещё довольно важный `payload`, это просто `{{config}}`, вот если прописать `config`, то в ответе должно получится, что то вроде вывода объектов из питона:
![[Pasted image 20260510195127.png]]
Действительно в ответе получаем объекты из питона и среди них видим флаг.
Кстати, если декодировать флаг из `base64`, то получается строка `simple_ssti`
`flag`:
`vshosh{czFtcGxlX3NzdGkK}`

Итак, что позволяет делать конфиг?
`{{config}}` - позволяет вытащить данные из конфигурации `Flask`.
У `Flask` в коде питона есть условно параметр `app` и через `app.config` мы можем задавать переменные, например `app.config["flag"] = 'vsosh{123}'`
И через шаблонизатор `{{config}}` мы можем получить все эти параметры `"flag"` и тд, а они нужны для того, чтобы в эти конфиги прописывать например доступ к базе данных, чтобы не создавать какие то отдельные переменные, потом просто удобно к конфигам обращаться и вызывать эти параметры `"flag"` и т.д, обычно туда прописывается доступ к базе данных, условно `"db_name"` ; `"db_password"` ; `"db_username"` и т.д
И вот через `{{config}}` мы можем все эти параметры извлекать, мы попытались извлечь и да флаг был просто прописан в параметр/переменную `SECRET_KEY`, а остальные символы, которые видно в выводе там `&#39` это знаки -  `апострофы` или `равно`, просто они отображаются в виде такого кода.

Как скорее всего задумывалось решение задачи разработчиками:
врядли разработчики задумывали такое легкое решение задачи, скорее всего в `HackTricks` нужно было искать пейлоад связанные с `jinja2` и там есть `.md` файл под названием `jinja-2-ssti`, в котором можно тоже поискать какой нибудь пейлоад:
например такой
![[Pasted image 20260511083621.png]]

`RCE - Remote Code Execution`, это тоже одна из уязвимостей, которая перерастает из `SSTI`
Откроем `BurpSuite` и попробуем реализовать этот `payload`
```payload
{{ config.__class__.from_envvar.__globals__.__builtins__.__import__("os").popen("ls").read() }}
```
Сначала разберём `payload`
1. `config`: Обращается к объекту конфигурации `Flask`
2. `__class__.from_envvar.__globals__.__builtins__`: Использует цепочку атрибутов Python, чтобы добраться до глобальных переменных и встроенных функций `__builtins__`
3. `__import__("os")`: Импортирует модуль для работы с операционной системой
4. `popen("ls").read()`: Выполняет команду терминала `ls` и считывает результат.

 Итак отправляем `payload` на сервер, тем самым эксплуатируем команду `ls` и получаем результат в ответе от сервера:![[Pasted image 20260511084733.png]]
 То есть, через такой питоновский код мы можем общаться с системой на сервере напрямую.
 Просмотрим содержимое файла `service.py`:
 ```payload
 {{ config.__class__.from_envvar.__globals__.__builtins__.__import__("os").popen("cat service.py").read() }}
 ```
 ![[Pasted image 20260511085255.png]]
 То, что прописано после `app.` мы получаем как раз таки через `conifg` и тем самым получаем содержимое переменное `secret_key`, в которой находится флаг.