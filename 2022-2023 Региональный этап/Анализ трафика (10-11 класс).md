```
## Анализ трафика (10-11 класс)
На Web-сервере орбитальной станции был зафиксирован подозрительный трафик, после чего управление ею было перехвачено злоумышленниками. Вскоре экипаж получил анонимное сообщение "Я слышал, космонавты очень умные. Докажите это. Я верну вам управление станцией, если сможете найти в фрагментах трафика (.pcapng) и расшифровать мой ключик. Иначе вы все сгорите в атмосфере! Ахахаха!". Помогите экипажу расследовать данный инцидент и вернуть управление станцией.

Цель работы: является исследование записи трафика (.pcapng).

Итог работы: нахождение и декодирование флага, доступ к которому является индикатором успешного решения задания.

Критерий оценки: предоставление правильного флага.

Рекомендуемые предустановленные утилиты: WireShark, python3
```

Решение:
<img width="1543" height="820" alt="image" src="https://github.com/user-attachments/assets/974d5671-cd6f-447b-81fe-b80addbe1cf8" />


открываем наш файл в программе `Wireshark` и предварительно видим в `protocol hierarchy`, что есть пакеты `Hypertext Transfer Protocol`, сортируем через поиск пакеты по `http` и немного пролистав вниз, замечаем `POST` запрос и ответ на него на какую то страницу с интересным названием, по названию видно, что это очевидный вектор и начинаем анализировать пакет `follow - http`

<img width="1122" height="788" alt="image" src="https://github.com/user-attachments/assets/dd440b1b-0759-40a9-a24b-a77de2e0250d" />


в открывшемся окне, можем заметить, что запрос отправлялся с агента `python-requests`. Также смотрим на `payload`, который использовался и понимаем, что флаг был закодирован используя криптографию, а именно сначала был отксорен значением `0x90`, а после закодирован в `base16` и в `base64`. Идём смотреть, что сервер отдал такому запросу.

<img width="1115" height="793" alt="image" src="https://github.com/user-attachments/assets/4f26abc6-9b99-431c-bbc7-5f2248874b0f" />


Видим наш закодированный текст в формате `base64`:
`RTZFM0ZGRTNGOEVCQThGMkE3RjRBOEEwQTRBNkE3QTVBMkYyQTVBMEYxQTdBNUE2RjNBN0EyQTZFRA==`

Дальше `Wireshark` нам не поможет, ведь нужно декодировать эту строку и из нее теперь извлекать флаг.

Открываем `python3` в `IDLE`.

Далее импортируем библиотеку `base64`; создаем переменную в которую вводим наше значение `base64`, а после декодируем значение этой переменной используя библиотеку.

Тоже самое проделываем с `base16`.

Далее получаем вот такую байтовую строку:
`b'\xe6\xe3\xff\xe3\xf8\xeb\xa8\xf2\xa7\xf4\xa8\xa0\xa4\xa6\xa7\xa5\xa2\xf2\xa5\xa0\xf1\xa7\xa5\xa6\xf3\xa7\xa2\xa6\xed'`

После этого берем нашу переменную с байтовой строкой и создаем цикл, в цикле же мы переводим наши байты в символы и производим операцию `XOR` с нашим ключом "`0x90`", а после этого прописываем `end=''`, чтобы питон вывел `print` в одну строчку(если этого не сделать, то каждый символ будет с новой строки), получается вот так:
<img width="699" height="708" alt="image" src="https://github.com/user-attachments/assets/07c1346a-199c-4f2a-b576-eab9a2c92fa0" />


**Получаем `ФЛАГ`:**
`*vsosh{8b7d8046752b50a756c726}*`
